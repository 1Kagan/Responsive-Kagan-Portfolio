import 'package:flutter/material.dart';

const primaryColor = Color.fromARGB(255, 238, 181, 24);
const secondaryColor = Color.fromARGB(255, 37, 37, 41);
const darkColor = Color(0xFF191923);
const bodyTextColor = Color.fromARGB(255, 253, 253, 255);
const bgColor = Color.fromARGB(255, 0, 0, 0);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); //
const maxWidth = 1920.0; // 
